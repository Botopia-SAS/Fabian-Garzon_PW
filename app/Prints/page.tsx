"use client";

import React from "react";
import Prints from "@/components/prints"; // Asegúrate de que la ruta sea correcta

const Page = () => {
  return (
    <div>
      <Prints />
    </div>
  );
};

export default Page;
